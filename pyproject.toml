[project]
name = "restaurant-queue"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "boto3>=1.40.14",
    "requests>=2.32.5",
]

[project.scripts]
create-table = "create_table:main"
