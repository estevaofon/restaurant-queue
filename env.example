# Exemplo de variáveis de ambiente para o projeto
# Copie este arquivo para configurar seu ambiente

# ===========================================
# AWS Amplify Environment Variables
# ===========================================
# Configure estas variáveis no AWS Amplify Console:
# App Settings → Environment Variables

# URL da API Gateway (OBRIGATÓRIO)
# Obter após: serverless deploy --stage <stage>
API_URL=https://abc123.execute-api.us-east-1.amazonaws.com/dev

# Stage/Ambiente (opcional)
STAGE=dev

# Região AWS (opcional)
AWS_DEFAULT_REGION=us-east-1

# Debug mode (opcional)
DEBUG=false

# Intervalo de atualização automática em ms (opcional)
AUTO_REFRESH_INTERVAL=30000

# Analytics habilitado (opcional)
ANALYTICS=false

# ===========================================
# Para Desenvolvimento Local
# ===========================================
# Para teste local, edite o arquivo: frontend/env.js
# Exemplo:
# window.ENV = {
#     API_URL: 'https://sua-api.execute-api.us-east-1.amazonaws.com/dev',
#     STAGE: 'dev',
#     REGION: 'us-east-1',
#     DEBUG: true
# };

# ===========================================
# Instruções de Deploy
# ===========================================
# 1. Deploy da API:
#    serverless deploy --stage dev
#
# 2. Copiar URL do output do deploy
#
# 3. Configurar no AWS Amplify:
#    Console → App → Environment Variables → API_URL
#
# 4. Redeploy automático do Amplify
